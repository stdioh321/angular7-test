import { ShowOnConsolePipe } from './show-on-console.pipe';

describe('ShowOnConsolePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowOnConsolePipe();
    expect(pipe).toBeTruthy();
  });
});
